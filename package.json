{
  "name": "station.lat-monorepo",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "author": "acerohernan",
  "description": "This is the main monorepo of Station.lat",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "eslint": "^8.40.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "server": "yarn workspace @station.lat/server",
    "config": "yarn workspace @station.lat/config",
    "web": "yarn workspace @station.lat/web"
  }
}
