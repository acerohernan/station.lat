{
  "name": "@station.lat/server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "@station.lat/config": "*",
    "@station.lat/core": "*",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^6.1.5",
    "passport": "^0.6.0",
    "passport-google-oauth20": "^2.0.0",
    "ts-node-dev": "^2.0.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.3",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/node": "^20.1.0",
    "@types/passport": "^1.0.12",
    "@types/passport-google-oauth20": "^2.0.11"
  },
  "scripts": {
    "dev": "NODE_ENV=development ts-node-dev --transpile-only --respawn src/start.ts",
    "build": "yarn build:clean && tsc",
    "build:clean": "rm -rf dist",
    "lint": "eslint src --fix",
    "format": "prettier --write src"
  }
}
