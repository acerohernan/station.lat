import { Card } from '@mui/material';
import CompanyLayout from '../components/layout';

const CompanySettingsPage = () => {
  return (
    <CompanyLayout>
      <Card sx={{ height: 300 }}>Settings Page</Card>
    </CompanyLayout>
  );
};

export default CompanySettingsPage;
